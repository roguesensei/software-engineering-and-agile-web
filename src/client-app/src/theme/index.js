import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		primary: {
			main: '#ff0000'
		},
		secondary: {
			main: '#ffffff',
		}
	},
	spacing: 8
});

export default theme;